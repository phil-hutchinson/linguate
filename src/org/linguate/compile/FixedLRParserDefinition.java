/*
 * This code has not bee released under any license.
 * In future, it will be released under an open-source license.
 */
package org.linguate.compile;

import java.util.*;


/**
 *
 * @author Phil Hutchinson
 */
public class FixedLRParserDefinition //implements LRParserDefinition
{
    /*
    private final ContextFreeGrammar grammar;
    
    private FixedLRParserDefinition(ContextFreeGrammar grammar)
    {
        this.grammar = grammar;
    }
            
    public static class Builder
    {
        private FixedLRParserDefinition outer;
        
        public Builder(ContextFreeGrammar grammar)
        {
            outer = new FixedLRParserDefinition(grammar);
        }
        
        public void AddState(LRParserAction.ActionType actionType)
        {
            
        }
        
        public FixedLRParserDefinition Finalize()
        {
            FixedLRParserDefinition result = outer;
            outer = null;
            return result;
        }
    }
    */
}
